# blog

Initial implementation was using client-side rendering (CSR).
Blog posts were fetched and rendered at runtime in the browser.

This approach was later replaced by a build-time SSG architecture
to improve SEO, stability, and performance.
